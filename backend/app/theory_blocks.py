# Теория для миссий E-Commerce из ECOMMERCE_THEORY_BLOCKS.md
# Ключ — mission_id в бэкенде (ecom-t1-001, ecom-t2-001, ...)

THEORY_BLOCKS = {
    "ecom-t1-001": {
        "title": "Призрачный товар — HTTP Status Codes",
        "content": """Почему возникает ошибка

HTTP-статус коды — это стандартизированный способ сообщить клиенту о результате запроса. Однако разработчики часто:

— Используют generic-обработчики: один обработчик для всех случаев возвращает 200 OK, даже когда ресурс не найден
— Путают «пустой результат» с «отсутствием ресурса»: запрос выполнился успешно (нашли 0 записей), но это не означает, что ресурс существует
— Копируют код без адаптации: шаблонный код из другого endpoint'а, где 200 был уместен

Почему это допускается

Фактор — Описание:
Спешка в разработке — «Работает — не трогай», статус-код кажется мелочью
Отсутствие спецификации — Нет чёткого контракта API, каждый разработчик решает по-своему
Недостаток тестирования — Тесты проверяют happy path, но не edge cases
Frontend компенсирует — Клиентское приложение само проверяет пустое тело

Влияние на продукт

— Клиентские приложения: неправильная обработка ошибок, кэширование «пустых» ответов как валидных, сломанная логика retry
— SEO и индексация: поисковики индексируют несуществующие страницы, дубликаты контента
— Мониторинг: невозможно отличить ошибки от успеха, ложные метрики
— Интеграции: партнёры получают некорректные данные, цепочка ошибок в микросервисах

Отраслевая статистика

По данным исследований API-качества, 23% публичных API возвращают некорректные HTTP-статусы для отсутствующих ресурсов. Это приводит к увеличению времени отладки интеграций на 40%.""",
    },
    "ecom-t1-002": {
        "title": "Метод не тот — HTTP Methods Validation",
        "content": """Почему возникает ошибка

REST (RFC 7231) и отраслевые практики требуют семантического использования HTTP-методов: GET — получение ресурса (идемпотентный, кэшируемый), POST — создание (не идемпотентный), PUT — замена, DELETE — удаление. Ошибки возникают когда:

— Роутинг настроен слишком широко: один handler принимает любой метод и возвращает 200 с данными
— Framework по умолчанию не проверяет метод: достаточно совпадения пути
— Middleware пропускает проверку: считают, что «клиент всегда шлёт GET»
— Legacy-клиенты использовали POST для всего: обратная совместимость без валидации

Почему это допускается

Фактор — Описание:
«Какая разница методом» — игнорируют семантику кэширования (GET кэшируется CDN, POST — нет), безопасность (CSRF по-разному для GET/POST), идемпотентность (GET можно повторять, POST — нет)
Отсутствие контракта — OpenAPI не проверяется в runtime, каждый разработчик решает по-своему
Тесты только happy path — проверяют GET, не проверяют POST к read-only endpoint

Влияние на продукт

— Кэширование: CDN и прокси кэшируют GET; если POST принимается как GET или возвращает 200, возможны некорректные кэши
— Безопасность: CSRF-атаки по-разному применимы к GET и POST; путаница методов ослабляет защиту
— Логирование и аудит: невозможно понять, что хотел сделать клиент (прочитать или изменить)
— Интеграции: партнёры полагаются на семантику методов; нарушение ломает retry и идемпотентность

Отраслевой стандарт

RFC 7231 (HTTP/1.1 Semantics): для ресурса, не поддерживающего метод, сервер должен вернуть 405 Method Not Allowed и заголовок Allow со списком допустимых методов. Игнорирование метода и возврат 200 — типичная ошибка в 15–20% публичных API по отраслевым обзорам.""",
    },
    "ecom-t1-003": {
        "title": "Пустая корзина — Required Fields Validation",
        "content": """Почему возникает ошибка

Валидация обязательных полей кажется простой, но в реальности встречаются несколько типичных ошибок:

— Разные слои валидации: JSON-схема проверяет наличие ключа, но не проверяет бизнес-правило (quantity > 0); бизнес-логика может не вызываться при пустом теле
— Nullable vs absent: поле quantity может быть null, отсутствовать, равно 0 или пустой строке — это четыре разных случая, которые часто обрабатывают одним условием
— Default values маскируют отсутствие: в коде пишут quantity = body.get('quantity', 0), и запрос без quantity создаёт позицию с нулевым количеством
— Partial updates: логику «обязательное поле» переносят только на PATCH, а POST создаёт запись без проверки

Почему это допускается

Фактор — Описание:
Типичный процесс — пишут happy path с quantity=5, добавляют if quantity, тестируют с валидными данными; в production приходят запросы без quantity или с quantity=0, default подставляется, баг обнаруживается через месяц
Спецификация не формализована — в OpenAPI поле помечено required, но сервер не проверяет или проверяет только «присутствует», а не «корректно»
Frontend «всегда шлёт» — считают, что клиент не отправит пустое значение, и не тестируют API напрямую

Влияние на продукт

— Невалидные записи в базе: корзина с позициями quantity=0, заказы с нулевой суммой
— Пользователи не понимают пустой заказ или дубликаты позиций
— Аналитика искажается: считают «добавления в корзину», а часть из них — мусор
— Рост обращений в поддержку: «не могу оформить заказ», «в корзине пусто, но показывается сумма»

Отраслевая практика

JSON Schema и OpenAPI рекомендуют помечать обязательные поля (required) и при необходимости minimum/exclusiveMinimum для числовых полей. Сервер должен возвращать 400 Bad Request с понятным сообщением (например, «quantity is required and must be positive»). Принятие отсутствующего или нулевого quantity — типичная ошибка в e-commerce API.""",
    },
    "ecom-t1-004": {
        "title": "Типы данных — Type Coercion",
        "content": """Почему возникает ошибка

В API ожидается число (integer) для quantity, но клиент может отправить строку: "5", "5шт", "5.0". Ошибки возникают когда:

— Парсер выполняет неявное преобразование: JSON допускает числа, но при парсинге "5шт" в одних языках получают 5 (parseInt), в других — ошибку
— Loose equality: проверка value == 5 срабатывает и для 5, и для "5" после приведения типов
— ORM или слой БД сам приводит тип при записи: строка обрезается или преобразуется в число без валидации на стороне API
— Разные локали: "5,5" (запятая как десятичный разделитель) в одном регионе парсится как 5, теряется дробная часть

Почему это допускается

Фактор — Описание:
«Гибкость важнее» — разработчики считают, что принимать и "5", и 5 удобнее для клиентов; не документируют строгий тип
Отсутствие схемы — без строгой JSON Schema (type: integer, без coercion) каждый endpoint реализует свой подход
Тесты с «нормальными» данными — тестируют quantity: 5, не тестируют quantity: "5шт"

Влияние на продукт

— Интернационализация: "5,5" (немецкий формат) парсится как 5 — теряется 0.5 единицы; в платёжных системах такие ошибки приводят к финансовым потерям
— Неочевидное поведение: quantity: "5шт" может стать 5 в одной версии API и 400 в другой — ломаются интеграции
— Реальные инциденты: известны случаи (в т.ч. в платёжных системах), когда type coercion приводил к неверным суммам и списаниям

Отраслевой стандарт

OpenAPI 3.0 явно задаёт тип (type: integer) для полей; неявное преобразование из строки не входит в стандарт. Рекомендуется возвращать 400 Bad Request с сообщением вроде «quantity must be an integer», если передан неверный тип. Строгая валидация типов снижает риски в финансах и логистике.""",
    },
    "ecom-t1-005": {
        "title": "Content-Type игнорируется — Content-Type Validation",
        "content": """Почему возникает ошибка

Заголовок Content-Type сообщает серверу формат тела запроса (application/json, application/x-www-form-urlencoded и т.д.). Ошибки возникают когда:

— Парсер «угадывает» формат: по содержимому тела (начинается с {) решают, что это JSON, и парсят без проверки заголовка
— Middleware не проверяет Content-Type: пропускает запрос дальше с любым заголовком
— Обратная совместимость: старые клиенты слали text/plain, решено принимать оба варианта
— Copy-paste конфигурации: в другом проекте принимали несколько форматов, скопировали без пересмотра

Почему это допускается

Фактор — Описание:
«Браузеры не всегда шлют Content-Type» — считают проблемой клиента, а не сервера; на практике API должны явно требовать формат
«Гибкость» — один endpoint принимает и JSON, и text/plain; это создаёт риски (см. ниже)
Валидация «замедляет» — отказ проверять заголовок ради скорости; безопасность при этом страдает

Влияние на продукт

— Content-Type Confusion: атакующий отправляет Content-Type: text/html с телом, содержащим скрипт; если сервер парсит тело как JSON и логирует его, а лог-viewer рендерит HTML — возможен XSS в админ-панели
— Непредсказуемость: один и тот же запрос с разным Content-Type может обрабатываться по-разному
— Нарушение контракта: клиенты и документация ожидают application/json; принятие text/plain ломает предположения об обработке ошибок (415 Unsupported Media Type)

Отраслевой стандарт

RFC 7231: клиент должен отправлять Content-Type для тела запроса. Сервер, не поддерживающий указанный тип, должен вернуть 415 Unsupported Media Type. Рекомендуется для JSON API явно требовать Content-Type: application/json и отклонять иные типы.""",
    },
    "ecom-t1-006": {
        "title": "Дублирование заказа — Idempotency",
        "content": """Почему возникает ошибка

Идемпотентность операции означает: повторное выполнение с теми же параметрами даёт тот же результат и не создаёт дубликат. Для POST /orders типичные ошибки:

— Нет хранилища ключей идемпотентности: каждый POST создаёт новый заказ, заголовок X-Idempotency-Key игнорируется
— Race condition: два запроса с одним ключом приходят почти одновременно, оба проходят проверку «ключа ещё нет» и создают два заказа
— Неверная генерация ключа на клиенте: ключ привязан к сессии, а не к корзине/действию, поэтому разные действия получают один ключ или наоборот
— TTL ключа слишком короткий: ключ удаляется через минуту, retry после таймаута создаёт второй заказ

Почему это допускается

Фактор — Описание:
«Пользователь не нажмёт дважды» — на практике пользователи двойной клик делают часто; при медленной сети или таймауте клиент повторяет запрос
Отсутствие требований — в ТЗ не прописана идемпотентность, разработчик не реализует проверку ключа
Сложность — нужен хранилище (Redis/БД) для ключей и TTL; считают избыточным для «простого» API

Влияние на продукт

— Дублирующие заказы: клиент платит дважды, получает два набора товаров или один и возврат
— Двойное списание, путаница в логистике и поддержке
— Потеря доверия и рост обращений в поддержку
— Отраслевые данные: у платёжных провайдеров до 0.5% транзакций — дубликаты из-за retry; идемпотентность обязательна для платежей и заказов

Отраслевой стандарт

Stripe, PayPal и др. требуют Idempotency-Key (или аналог) для создания платежей и заказов. RFC и лучшие практики (e.g. Stripe API) рекомендуют: при повторном POST с тем же ключом возвращать тот же результат (200/201 с теми же данными), а не создавать новую сущность.""",
    },
    "ecom-t2-001": {
        "title": "Граница количества — Boundary Values",
        "content": """Почему возникает ошибка

Off-by-one (OBO) — одна из старейших и распространённых ошибок в программировании. В контексте quantity типично:

— Путаница < и ≤: требование «максимум 99» реализуют как if (quantity < 100), тогда quantity=100 проходит валидацию
— Индексация и циклы: границы диапазона (1..99) проверяют нестрого с одной стороны
— Документация размыта: «до 99» трактуют как «меньше 99» или «не больше 100» по-разному
— Тесты не покрывают границу: тестируют quantity=50, не тестируют 99 и 100

Почему это допускается

Фактор — Описание:
Требование «максимум 99 единиц» часто кодируют как if (quantity < 100) — кажется эквивалентным, но 100 не отклоняется
Граничное значение «неудобно» тестировать — забывают добавить тест на 100 и 101
Влияние кажется малым — одна лишняя единица в корзине; на деле это обход бизнес-правил (лимиты, скидки от количества)

Влияние на продукт

— Лимиты заказа обходятся: клиент может добавить 100 вместо разрешённых 99
— Скидки по количеству: «от 10 штук скидка» может срабатывать от 11 или от 9 из-за OBO
— Пагинация и лимиты в других модулях: та же ошибка в других местах кода

Отраслевая практика

Тестирование граничных значений (Boundary Value Analysis): для диапазона [1, 99] обязательно проверять 0, 1, 99, 100. Стандарт — явная проверка (quantity >= 1 && quantity <= 99) и возврат 400 при выходе за границы.""",
    },
    "ecom-t3-001": {
        "title": "Машина состояний заказа — State Machine",
        "content": """Почему возникает ошибка

Конечный автомат (state machine) задаёт допустимые переходы состояний заказа: CREATED → PAID → PROCESSING → SHIPPED → DELIVERED. Отмена (CANCELLED) допустима только до определённого шага. Ошибки возникают когда:

— Нет явной модели переходов: статусы меняют в разных местах кода без централизованной проверки
— Не все переходы учтены: проверяют «не PAID», но не проверяют DELIVERED отдельно
— Race condition: два запроса (отмена и обновление статуса) обрабатываются одновременно
— Backdoor или админский API: отмена «в обход» для поддержки, но endpoint доступен и обычному пользователю

Почему это допускается

Фактор — Описание:
Эволюция кода: изначально было два статуса, потом добавили цепочку; проверки добавляли точечно, не пересматривая всю модель
Спагетти-логика: «если не delivered» размазано по разным функциям, в одном месте забыли проверить
Требования не формализованы: в ТЗ написано «доставленный не отменять», но нет таблицы переходов

Влияние на продукт

— Отмена доставленного заказа: возврат денег за уже полученный товар, мошенничество
— Путаница в логистике и бухгалтерии: заказ то отменён, то доставлен
— Двойные возвраты и репутационные потери

Отраслевая практика

Рекомендуется явная state machine (таблица допустимых переходов) и единая функция перехода статуса. Для заказа в статусе DELIVERED метод отмены должен возвращать 400 или 409 с сообщением «Order already delivered».""",
    },
    "ecom-t4-001": {
        "title": "Чужой заказ — IDOR Vulnerability",
        "content": """Почему возникает ошибка

IDOR (Insecure Direct Object Reference) — уязвимость, когда доступ к объекту определяется только его идентификатором в URL или теле запроса. Сервер не проверяет, принадлежит ли заказ текущему пользователю. Типичные причины:

— Отсутствие проверки владельца: GET /orders/123 возвращает заказ по id без сравнения order.user_id с текущим пользователем
— Проверка только аутентификации: middleware проверяет, что пользователь залогинен, но не проверяет авторизацию на конкретный ресурс
— Copy-paste: скопировали endpoint из админки или другого сервиса без добавления проверки владельца
— Предположение «никто не угадает ID»: UUID или большой числовой id считают «безопасными», но ID можно получить из истории, писем, утечек

Почему это допускается

Фактор — Описание:
«Middleware проверит» — но middleware проверяет только наличие токена, не связь ресурс–пользователь
Централизация авторизации отложена — «потом добавим», в итоге забывают
Легаси: старый API не имел мультитенантности, при добавлении пользователей проверку не внедрили

Влияние на продукт

— Утечка данных: просмотр чужих заказов, адресов, сумм, способов оплаты
— Нарушение конфиденциальности и регуляторики (GDPR и др.)
— IDOR входит в OWASP Top 10 (Broken Access Control); по отраслевым данным — причина значительной доли утечек через API

Отраслевой стандарт

Каждый запрос к ресурсу «по id» должен проверять: ресурс принадлежит текущему пользователю (или роли). При несовпадении — 403 Forbidden. Нельзя полагаться на «сложность угадывания» ID.""",
    },
    "ecom-t5-001": {
        "title": "Промокод-брутфорс — Rate Limiting",
        "content": """Почему возникает ошибка

Endpoint проверки промокода (например, POST с телом {code: "XXXX-XXXX"}) принимает запросы без ограничения частоты. Атакующий может перебирать коды перебором или по словарю. Типичные причины:

— Отсутствие rate limiting: нет лимита на количество запросов с одного IP или пользователя в минуту
— Слабые коды: промокоды короткие или предсказуемые (SAVE-2024, BLACK-FRI), словарь из 10k слов даёт миллионы комбинаций, но при 1000 запросов/сек перебор за часы
— Нет капчи или задержки после N неудачных попыток
— Логи не мониторятся: массовый перебор не обнаруживают

Почему это допускается

Фактор — Описание:
«API внутренний» — считают, что endpoint не найдут; на практике его находят в клиенте или документации
«Промокоды сложные» — на деле люди используют словарные слова и даты
«Rate limit дорогой» — Redis-счётчик и проверка занимают миллисекунды; отказ от лимита создаёт риск
WAF не знает бизнес-логику — блокирует по сигнатурам, а не по «много проверок кода»

Влияние на продукт

— Массовое использование подобранных промокодов, прямые финансовые потери
— Нагрузка на сервер и БД при миллионах запросов
— Репутационный ущерб при утечке кодов

Отраслевая практика

Рекомендуется: rate limit на endpoint проверки кода (например, 5–10 запросов в минуту с IP или на пользователя), капча после нескольких неудачных попыток, мониторинг аномальной активности. Промокоды должны быть криптостойкими или одноразовыми.""",
    },
    # T2 E-Commerce (E-Commerce_T2_Missions_TZ)
    "ecom-t2-price-boundary": {
        "title": "Граничные значения цен",
        "content": """Почему возникает ошибка

В большинстве баз данных числовые типы имеют ограничения:
- INT (4 bytes): -2,147,483,648 до 2,147,483,647
- BIGINT (8 bytes): до 9.2 × 10^18
- DECIMAL(10,2): 10 цифр, 2 после запятой

При превышении лимита происходит:
1. Overflow — число "переворачивается" в отрицательное
2. Truncation — отбрасывается часть данных
3. Silent failure — ошибка не логируется

Влияние на продукт:
- Товары с отрицательной ценой
- Некорректные суммы заказов
- Финансовые потери""",
    },
    "ecom-t2-quantity-limits": {
        "title": "Граничные значения количества",
        "content": """Почему возникает ошибка

Лимиты количества (min/max на один запрос, лимит по корзине, проверка остатка на складе) часто реализуются неполно: проверяют только один запрос, не агрегат по корзине; не сверяют с stock; принимают float вместо integer.

Влияние на продукт:
- Корзина с нулевым или отрицательным количеством
- Заказ больше остатка на складе
- Обход лимита корзины множественными запросами""",
    },
    "ecom-t2-pagination-abuse": {
        "title": "Пагинация и сортировка",
        "content": """Почему возникает ошибка

Пагинация (page, limit, offset) и сортировка (sort, order) уязвимы: отрицательная страница или offset overflow вызывают 500; limit без ограничения — DoS; подстановка sort в SQL без валидации — SQL injection; сортировка по внутреннему полю раскрывает данные.

Влияние на продукт:
- DoS при большом limit или offset
- SQL injection через параметр sort
- Утечка внутренних полей через sort""",
    },
    "ecom-t2-date-validation": {
        "title": "Валидация форматов дат",
        "content": """Почему возникает ошибка

Даты без нормализации timezone, неверный формат (DD-MM-YYYY вместо ISO), дата в далёком будущем или прошедшая дата для промо, невисокосная дата 29 февраля — типичные причины багов в e-commerce.

Почему это допускается

Фактор — Описание:
Разные локали и форматы — разработчики из разных стран используют разные форматы дат (DD-MM-YYYY, MM-DD-YYYY, ISO 8601), не унифицируют
Отсутствие валидации диапазона — проверяют только формат, но не проверяют разумность даты (не в прошлом для заказа, не через 100 лет)
Timezone игнорируется — считают, что все даты в UTC или локальном времени, не нормализуют
Тесты с «нормальными» датами — тестируют текущую дату ± несколько дней, не тестируют граничные случаи

Влияние на продукт

— Заказы с датой доставки в будущем через годы
— Промо с прошедшей датой окончания
— Bypass срока возврата через timezone
— Путаница в логистике и бухгалтерии из-за неверных дат

Отраслевая практика

ISO 8601 рекомендует формат YYYY-MM-DD для дат и явное указание timezone. Рекомендуется валидация диапазона дат (например, дата доставки не раньше сегодня и не позже года вперёд), нормализация timezone перед сохранением, проверка високосных годов для 29 февраля.""",
    },
    "ecom-t2-string-length": {
        "title": "Валидация строковых данных",
        "content": """Почему возникает ошибка

Длина строк (max 255), пустая строка для обязательного поля, null byte и спецсимволы в имени, только пробелы — без валидации приводят к truncation, пустым названиям, SQL injection или ошибкам БД.

Влияние на продукт:
- Товары без названия или с truncated именем
- SQL injection через поле name
- Некорректное отображение из-за null byte""",
    },
    "ecom-t2-discount-calc": {
        "title": "Логика расчёта скидок",
        "content": """Почему возникает ошибка

Сумма скидок > 100% даёт отрицательный total; несколько промокодов применяются без лимита; истёкший код принимается; округление по позициям до нуля; отрицательная цена; loyalty points без проверки баланса — типичные баги в расчётах.

Влияние на продукт:
- Отрицательная сумма заказа
- Применение истёкших кодов
- Финансовые потери из-за округления и overflow""",
    },
    "ecom-t2-search-filter": {
        "title": "Поиск и фильтрация",
        "content": """Почему возникает ошибка

Пустой поисковый запрос, спецсимволы в q, несуществующая категория (filter bypass), невалидный boolean для inStock, NoSQL injection через q — приводят к 500, обходу фильтров или возврату всех данных.

Влияние на продукт:
- DoS при пустом или специальном q
- Утечка всех товаров при injection
- Неверные результаты при невалидных фильтрах""",
    },
}


def get_theory(mission_id: str) -> dict:
    """Возвращает {title, content} для миссии или пустые строки."""
    block = THEORY_BLOCKS.get(mission_id)
    if not block:
        return {"title": "", "content": ""}
    return {"title": block["title"], "content": block["content"]}
