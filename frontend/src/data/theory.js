/**
 * Теоретические материалы по тирам
 * Доступ к разделам открывается по мере решения миссий (80% багов в предыдущем тире хотя бы в одном домене)
 */

export const T1_THEORY = `# 📚 Теория T1: Основы тестирования REST API

> **Уровень:** Beginner (T1)  
> **Цель:** Понять фундаментальные концепции работы с API для эффективного поиска дефектов

---

## Введение

Добро пожаловать в мир API-тестирования! Прежде чем искать баги, нужно понять, как работает то, что мы тестируем. В этом разделе вы изучите базовые концепции, которые станут вашим фундаментом для исследования любого API.

---

## 1. Что такое API?

**API (Application Programming Interface)** — это способ взаимодействия между программами. Представьте API как официанта в ресторане: вы (клиент) делаете заказ, официант (API) передаёт его на кухню (сервер), а затем приносит вам готовое блюдо (ответ).

В контексте веб-приложений, когда вы открываете мобильное приложение банка и смотрите баланс — приложение отправляет запрос к API банка, а API возвращает данные о вашем счёте.

### REST API

**REST (Representational State Transfer)** — архитектурный стиль для построения API. Большинство современных веб-сервисов используют именно REST API. Его ключевые принципы:

- **Клиент-серверная архитектура** — клиент и сервер независимы
- **Stateless (без состояния)** — каждый запрос содержит всю необходимую информацию
- **Единообразие интерфейса** — стандартизированные методы и форматы

---

## 2. Эндпоинты (Endpoints)

**Эндпоинт** — это конкретный URL-адрес, по которому клиент может обратиться к серверу для выполнения определённой операции.

### Структура эндпоинта

\`\`\`
https://api.example.com/api/v1/resources/{id}/subresources
└──────────┬─────────┘ └──┬──┘ └───┬───┘ └─┬┘ └─────┬─────┘
       Base URL      Version  Resource  ID   Sub-resource
\`\`\`

**Компоненты:**

| Часть | Пример | Описание |
|-------|--------|----------|
| Base URL | \`https://api.example.com\` | Адрес сервера |
| Version | \`/api/v1\` | Версия API |
| Resource | \`/resources\` | Тип ресурса (обычно во множественном числе) |
| ID | \`/{id}\` | Идентификатор конкретного ресурса |
| Sub-resource | \`/subresources\` | Связанные вложенные ресурсы |

### Примеры эндпоинтов

Представим API системы управления библиотекой:

\`\`\`
GET    /api/v1/books              → Получить список всех книг
GET    /api/v1/books/42           → Получить книгу с ID=42
POST   /api/v1/books              → Создать новую книгу
PUT    /api/v1/books/42           → Обновить книгу с ID=42
DELETE /api/v1/books/42           → Удалить книгу с ID=42
GET    /api/v1/books/42/reviews   → Получить отзывы на книгу 42
\`\`\`

### Query Parameters (Параметры запроса)

Позволяют фильтровать, сортировать и пагинировать данные:

\`\`\`
GET /api/v1/books?author=Pushkin&year=1833&limit=10&offset=0
                  └─────────────────┬─────────────────────┘
                          Query Parameters
\`\`\`

| Параметр | Значение | Назначение |
|----------|----------|------------|
| \`author\` | Pushkin | Фильтр по автору |
| \`year\` | 1833 | Фильтр по году |
| \`limit\` | 10 | Количество записей на странице |
| \`offset\` | 0 | Смещение (пропустить N записей) |

---

## 3. HTTP-методы

HTTP-методы (или «глаголы») определяют, какое действие нужно выполнить над ресурсом.

### Основные методы

| Метод | Действие | Идемпотентность | Безопасность |
|-------|----------|-----------------|--------------|
| **GET** | Получить данные | ✅ Да | ✅ Да |
| **POST** | Создать ресурс | ❌ Нет | ❌ Нет |
| **PUT** | Полностью заменить ресурс | ✅ Да | ❌ Нет |
| **PATCH** | Частично обновить ресурс | ❌ Нет | ❌ Нет |
| **DELETE** | Удалить ресурс | ✅ Да | ❌ Нет |

**Идемпотентность** означает, что многократное выполнение одного и того же запроса даёт тот же результат, что и однократное.

**Безопасность** означает, что метод не изменяет состояние сервера.

### GET — Получение данных

\`\`\`http
GET /api/v1/books/42 HTTP/1.1
Host: api.library.com
Accept: application/json
\`\`\`

Сервер должен вернуть данные, не изменяя их.

### POST — Создание ресурса

\`\`\`http
POST /api/v1/books HTTP/1.1
Host: api.library.com
Content-Type: application/json

{
  "title": "Евгений Онегин",
  "author": "Александр Пушкин",
  "year": 1833,
  "isbn": "978-5-17-090000-0"
}
\`\`\`

Сервер создаёт новый ресурс и возвращает его с присвоенным ID.

### PUT — Полная замена ресурса

\`\`\`http
PUT /api/v1/books/42 HTTP/1.1
Host: api.library.com
Content-Type: application/json

{
  "title": "Евгений Онегин",
  "author": "Александр Сергеевич Пушкин",
  "year": 1833,
  "isbn": "978-5-17-090000-0",
  "pages": 448
}
\`\`\`

**Важно:** PUT требует передачи ВСЕХ полей ресурса. Если поле не передано — оно может быть сброшено.

### PATCH — Частичное обновление

\`\`\`http
PATCH /api/v1/books/42 HTTP/1.1
Host: api.library.com
Content-Type: application/json

{
  "pages": 448
}
\`\`\`

PATCH обновляет только указанные поля, остальные остаются без изменений.

### DELETE — Удаление ресурса

\`\`\`http
DELETE /api/v1/books/42 HTTP/1.1
Host: api.library.com
Authorization: Bearer eyJhbGciOiJIUzI1NiIs...
\`\`\`

---

## 4. HTTP-заголовки (Headers)

Заголовки передают метаинформацию о запросе и ответе.

### Основные заголовки запроса

| Заголовок | Назначение | Пример |
|-----------|------------|--------|
| \`Content-Type\` | Формат тела запроса | \`application/json\` |
| \`Accept\` | Ожидаемый формат ответа | \`application/json\` |
| \`Authorization\` | Аутентификация | \`Bearer token123...\` |
| \`User-Agent\` | Информация о клиенте | \`MyApp/1.0\` |

### Пример полного запроса с заголовками

\`\`\`http
POST /api/v1/reservations HTTP/1.1
Host: api.library.com
Content-Type: application/json
Accept: application/json
Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
X-Request-ID: req-abc-123

{
  "bookId": 42,
  "userId": "user-789",
  "reservationDate": "2026-02-15"
}
\`\`\`

---

## 5. Тело запроса (Request Body)

Тело запроса содержит данные, отправляемые на сервер. Наиболее распространённый формат — **JSON**.

### Структура JSON

\`\`\`json
{
  "string": "текстовое значение",
  "number": 42,
  "decimal": 99.99,
  "boolean": true,
  "null": null,
  "array": [1, 2, 3],
  "object": {
    "nested": "value"
  }
}
\`\`\`

### Типичное тело запроса

\`\`\`json
{
  "title": "Мастер и Маргарита",
  "author": {
    "firstName": "Михаил",
    "lastName": "Булгаков"
  },
  "year": 1967,
  "genres": ["роман", "фантастика", "сатира"],
  "available": true,
  "price": 599.00
}
\`\`\`

---

## 6. HTTP-коды ответа (Status Codes)

Код ответа сообщает клиенту о результате обработки запроса.

### Категории кодов

| Диапазон | Категория | Описание |
|----------|-----------|----------|
| **1xx** | Informational | Информационные (редко используются) |
| **2xx** | Success | Успешное выполнение |
| **3xx** | Redirection | Перенаправление |
| **4xx** | Client Error | Ошибка на стороне клиента |
| **5xx** | Server Error | Ошибка на стороне сервера |

### Успешные ответы (2xx)

| Код | Название | Когда используется |
|-----|----------|-------------------|
| **200** | OK | Успешный GET, PUT, PATCH |
| **201** | Created | Успешный POST (ресурс создан) |
| **204** | No Content | Успешный DELETE (нет тела ответа) |

### Ошибки клиента (4xx)

| Код | Название | Когда используется |
|-----|----------|-------------------|
| **400** | Bad Request | Некорректный синтаксис запроса |
| **401** | Unauthorized | Требуется аутентификация |
| **403** | Forbidden | Доступ запрещён (нет прав) |
| **404** | Not Found | Ресурс не найден |
| **405** | Method Not Allowed | Метод не поддерживается |
| **409** | Conflict | Конфликт (например, дубликат) |
| **422** | Unprocessable Entity | Семантическая ошибка в данных |

### Ошибки сервера (5xx)

| Код | Название | Когда используется |
|-----|----------|-------------------|
| **500** | Internal Server Error | Необработанная ошибка сервера |
| **502** | Bad Gateway | Ошибка прокси/gateway |
| **503** | Service Unavailable | Сервис временно недоступен |

### Пример успешного ответа

\`\`\`http
HTTP/1.1 201 Created
Content-Type: application/json
Location: /api/v1/books/143

{
  "id": 143,
  "title": "Мастер и Маргарита",
  "author": "Михаил Булгаков",
  "createdAt": "2026-02-04T10:30:00Z"
}
\`\`\`

### Пример ответа с ошибкой

\`\`\`http
HTTP/1.1 400 Bad Request
Content-Type: application/json

{
  "error": "VALIDATION_ERROR",
  "message": "Invalid request body",
  "details": [
    {
      "field": "year",
      "message": "Year must be a positive integer"
    }
  ]
}
\`\`\`

---

## 7. Тело ответа (Response Body)

Сервер возвращает данные в теле ответа. Формат обычно указан в заголовке \`Content-Type\`.

### Типичный успешный ответ (один объект)

\`\`\`json
{
  "id": 42,
  "title": "Война и мир",
  "author": "Лев Толстой",
  "year": 1869,
  "available": true,
  "borrowCount": 156
}
\`\`\`

### Типичный успешный ответ (коллекция)

\`\`\`json
{
  "data": [
    { "id": 1, "title": "Книга 1" },
    { "id": 2, "title": "Книга 2" }
  ],
  "pagination": {
    "total": 100,
    "limit": 10,
    "offset": 0,
    "hasMore": true
  }
}
\`\`\`

### Типичный ответ с ошибкой

\`\`\`json
{
  "error": {
    "code": "BOOK_NOT_FOUND",
    "message": "Book with ID 999 not found",
    "timestamp": "2026-02-04T10:30:00Z",
    "path": "/api/v1/books/999"
  }
}
\`\`\`

---

## 8. Аутентификация и авторизация

### Аутентификация vs Авторизация

- **Аутентификация** — подтверждение личности («Кто вы?»)
- **Авторизация** — проверка прав доступа («Что вам разрешено?»)

### Распространённые методы аутентификации

**Bearer Token (JWT)**
\`\`\`http
Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
\`\`\`

**API Key**
\`\`\`http
X-API-Key: sk_live_abc123def456
\`\`\`

**Basic Auth**
\`\`\`http
Authorization: Basic dXNlcm5hbWU6cGFzc3dvcmQ=
\`\`\`

---

## 9. Инструменты для работы с API

### cURL (командная строка)

\`\`\`bash
# GET запрос
curl -X GET "https://api.example.com/books/42" \\
  -H "Accept: application/json"

# POST запрос
curl -X POST "https://api.example.com/books" \\
  -H "Content-Type: application/json" \\
  -H "Authorization: Bearer token123" \\
  -d '{"title": "New Book", "author": "Author Name"}'
\`\`\`

### Postman / Insomnia

Графические инструменты для отправки HTTP-запросов с удобным интерфейсом.

---

## 10. Что искать при тестировании API?

Как тестировщик, вы исследуете поведение API в различных условиях. Вот направления для размышления:

### Вопросы о входных данных

- Что происходит, если обязательное поле отсутствует?
- Как API реагирует на данные неожиданного типа?
- Где находятся границы допустимых значений?
- Что если данные выходят за эти границы?

### Вопросы о бизнес-логике

- Соблюдается ли логическая последовательность операций?
- Можно ли выполнить действие, которое не должно быть разрешено?
- Как система обрабатывает одновременные запросы?

### Вопросы о безопасности

- Могу ли я получить доступ к чужим данным?
- Что если я не передам токен авторизации?
- Проверяются ли права на выполнение операции?

### Вопросы о состояниях

- Что происходит при повторной отправке того же запроса?
- Как система ведёт себя при невалидных переходах состояний?
- Корректно ли обрабатываются граничные случаи?

---

## 11. Практический пример: API бронирования

Рассмотрим гипотетический API системы бронирования переговорных комнат.

### Спецификация (OpenAPI фрагмент)

\`\`\`yaml
/api/v1/rooms/{roomId}/bookings:
  post:
    summary: Create a new booking
    parameters:
      - name: roomId
        in: path
        required: true
        schema:
          type: integer
    requestBody:
      content:
        application/json:
          schema:
            type: object
            required:
              - startTime
              - endTime
              - userId
            properties:
              startTime:
                type: string
                format: date-time
              endTime:
                type: string
                format: date-time
              userId:
                type: string
              notes:
                type: string
                maxLength: 500
    responses:
      201:
        description: Booking created successfully
      400:
        description: Invalid request
      409:
        description: Time slot already booked
\`\`\`

### Примеры запросов

**Корректный запрос:**

\`\`\`http
POST /api/v1/rooms/5/bookings HTTP/1.1
Host: api.booking.com
Content-Type: application/json
Authorization: Bearer eyJhbG...

{
  "startTime": "2026-02-05T10:00:00Z",
  "endTime": "2026-02-05T11:00:00Z",
  "userId": "user-123",
  "notes": "Team meeting"
}
\`\`\`

**Ответ:**

\`\`\`http
HTTP/1.1 201 Created
Content-Type: application/json

{
  "id": "booking-456",
  "roomId": 5,
  "startTime": "2026-02-05T10:00:00Z",
  "endTime": "2026-02-05T11:00:00Z",
  "userId": "user-123",
  "status": "confirmed"
}
\`\`\`

### Вопросы для исследования

Изучая эту спецификацию, тестировщик может задуматься:

- Что произойдёт, если \`endTime\` раньше \`startTime\`?
- Как система отреагирует на бронирование в прошлом?
- Валидируется ли \`roomId\` на существование?
- Можно ли создать бронирование на 0 минут?
- Что если два пользователя одновременно забронируют один слот?
- Проверяется ли, что \`userId\` в теле соответствует токену?

Эти вопросы направляют исследование, но ответы нужно найти экспериментально.

---

## 12. Чек-лист исследователя API

Перед началом тестирования убедитесь, что вы понимаете:

- [ ] Какие эндпоинты доступны
- [ ] Какие HTTP-методы поддерживаются
- [ ] Какие поля обязательны, какие опциональны
- [ ] Какие типы данных ожидаются
- [ ] Какие коды ответа задокументированы
- [ ] Требуется ли аутентификация

Во время тестирования исследуйте:

- [ ] Поведение на границах допустимых значений
- [ ] Реакцию на отсутствующие и лишние поля
- [ ] Обработку данных неправильного типа
- [ ] Последовательность операций
- [ ] Поведение при повторных запросах

---

## Заключение

Теперь у вас есть базовые знания для начала исследования API. Помните:

> **«Документация описывает ожидаемое поведение. Ваша задача — найти, где реальность расходится с ожиданиями.»**

Не бойтесь экспериментировать. Каждый неожиданный ответ сервера — это потенциальная находка. Успешного тестирования!

---

## Полезные ресурсы

- [HTTP Status Codes](https://httpstatuses.com) — справочник по кодам ответа
- [JSON Specification](https://www.json.org) — формат JSON
- [REST API Tutorial](https://restfulapi.net) — принципы REST

---

*Этот теоретический блок является частью QA Training Platform. Материал предназначен для формирования фундаментального понимания API-тестирования.*`;

// T2: Граничные значения и форматы данных
export const T2_THEORY = `# 📚 Теория T2: Граничные значения и форматы данных

> **Уровень:** Intermediate (T2)  
> **Prerequisite:** Завершено 80% миссий T1  
> **Цель:** Освоить техники тестирования граничных условий и форматов данных

---

## Введение

Вы освоили базовые концепции API в T1. Теперь пришло время заглянуть глубже — туда, где живут самые коварные баги. T2 фокусируется на **граничных значениях** и **форматах данных** — областях, где разработчики чаще всего допускают ошибки.

> **«Большинство багов прячутся на границах»**  
> — Борис Бейзер, классик тестирования ПО

---

## 1. Анализ граничных значений (Boundary Value Analysis)

### Почему границы важны?

Каждое поле ввода имеет допустимый диапазон значений. На границах этого диапазона код часто ведёт себя неожиданно:

\`\`\`
                    Допустимый диапазон
                 ◄────────────────────────►
    ─────────────┬────────────────────────┬─────────────
                MIN                      MAX
                 ▲                        ▲
                 │                        │
           Граничные точки — места потенциальных багов
\`\`\`

### Классическая техника: MIN, MIN-1, MIN+1, MAX, MAX-1, MAX+1

Для каждой границы проверяйте три точки:

| Точка | Описание | Ожидаемое поведение |
|-------|----------|---------------------|
| **MIN-1** | Значение ниже минимума | Ошибка валидации |
| **MIN** | Минимально допустимое | Успешная обработка |
| **MIN+1** | Чуть выше минимума | Успешная обработка |
| **MAX-1** | Чуть ниже максимума | Успешная обработка |
| **MAX** | Максимально допустимое | Успешная обработка |
| **MAX+1** | Значение выше максимума | Ошибка валидации |

### Пример: Система рейтингов

Представим API для оценки курсов, где рейтинг должен быть от 1 до 5:

\`\`\`yaml
# OpenAPI спецификация
rating:
  type: integer
  minimum: 1
  maximum: 5
\`\`\`

**Граничные значения для тестирования:**

| Значение | Категория | Ожидание |
|----------|-----------|----------|
| 0 | MIN-1 | 400 Bad Request |
| 1 | MIN | 200 OK |
| 2 | MIN+1 | 200 OK |
| 4 | MAX-1 | 200 OK |
| 5 | MAX | 200 OK |
| 6 | MAX+1 | 400 Bad Request |

---

## 2. Числовые типы и их ограничения

### Целые числа (Integers)

Компьютеры хранят числа в ограниченном количестве битов. Это создаёт жёсткие границы:

| Тип | Размер | Минимум | Максимум |
|-----|--------|---------|----------|
| INT8 (tinyint) | 1 байт | -128 | 127 |
| INT16 (smallint) | 2 байта | -32,768 | 32,767 |
| **INT32 (int)** | 4 байта | **-2,147,483,648** | **2,147,483,647** |
| INT64 (bigint) | 8 байт | -9.2 × 10¹⁸ | 9.2 × 10¹⁸ |
| UINT32 (unsigned) | 4 байта | 0 | 4,294,967,295 |

### Integer Overflow — переполнение

Что происходит, когда число превышает максимум?

\`\`\`
MAX_INT32 = 2,147,483,647

2,147,483,647 + 1 = ???
\`\`\`

В зависимости от языка и настроек:
- **Wraparound:** Число «переворачивается» → \`-2,147,483,648\`
- **Saturation:** Остаётся на максимуме → \`2,147,483,647\`
- **Exception:** Выбрасывается ошибка
- **Undefined behavior:** Непредсказуемое поведение

### Реальный пример из практики

В 2014 году видео "Gangnam Style" на YouTube набрало более 2,147,483,647 просмотров. Счётчик был 32-битным и грозил переполниться. Google пришлось срочно переходить на 64-битные числа.

### Числа с плавающей точкой (Floating Point)

Десятичные числа хранятся приближённо, что создаёт проблемы точности:

\`\`\`python
# Python демонстрация
>>> 0.1 + 0.2
0.30000000000000004

>>> 0.1 + 0.2 == 0.3
False
\`\`\`

### Проблема точности в финансах

Представим расчёт суммы заказа:

\`\`\`
Товар A: $19.99
Товар B: $29.99
Товар C: $49.99
─────────────────
Ожидаемый итог: $99.97

При использовании float:
19.99 + 29.99 + 49.99 = 99.97000000000001
\`\`\`

Расхождение в копейках при миллионах транзакций превращается в серьёзные суммы.

### Решение: Decimal или целые числа

Финансовые системы часто хранят суммы в минимальных единицах (копейках, центах):

\`\`\`
$99.97 → 9997 центов (integer)
\`\`\`

### Граничные значения для чисел с плавающей точкой

| Значение | Описание |
|----------|----------|
| \`0.0\` | Ноль |
| \`-0.0\` | Отрицательный ноль (да, такое есть!) |
| \`Infinity\` | Бесконечность |
| \`-Infinity\` | Отрицательная бесконечность |
| \`NaN\` | Not a Number (результат 0/0) |
| \`1e308\` | Очень большое число |
| \`1e-308\` | Очень маленькое число |

---

## 3. Форматы дат и времени

Даты — один из самых коварных типов данных. Разные системы интерпретируют их по-разному.

### ISO 8601 — международный стандарт

\`\`\`
2026-02-15T14:30:00Z
│    │  │  │  │  │ └─ Z = UTC (Zulu time)
│    │  │  │  │  └─── Секунды
│    │  │  │  └────── Минуты
│    │  │  └───────── Часы
│    │  └──────────── День
│    └─────────────── Месяц
└──────────────────── Год
\`\`\`

### Временные зоны — источник багов

\`\`\`
2026-02-15T14:30:00Z        → UTC (0 часов)
2026-02-15T14:30:00+05:00   → UTC+5 (Астана)
2026-02-15T14:30:00-08:00   → UTC-8 (Лос-Анджелес)
2026-02-15T14:30:00+14:00   → UTC+14 (Киритимати)
\`\`\`

**Ключевой вопрос:** Если срок истекает 15 февраля, в какой временной зоне это проверяется?

### Граничные даты для тестирования

| Дата | Особенность |
|------|-------------|
| \`2024-02-29\` | Високосный год (валидно) |
| \`2025-02-29\` | Не високосный год (невалидно!) |
| \`2000-02-29\` | Високосный (делится на 400) |
| \`1900-02-29\` | Не високосный (делится на 100, но не на 400) |
| \`2026-12-31\` | Последний день года |
| \`2027-01-01\` | Первый день года |
| \`1970-01-01\` | Unix Epoch (начало времён в Unix) |
| \`2038-01-19\` | Y2K38 проблема (overflow 32-bit Unix time) |
| \`9999-12-31\` | Далёкое будущее |
| \`0001-01-01\` | Начало нашей эры |

### Проблема Y2K38

32-битное Unix время (секунды с 1970-01-01) переполнится 19 января 2038 года:

\`\`\`
MAX_INT32 секунд = 2,147,483,647
1970-01-01 + 2,147,483,647 сек = 2038-01-19 03:14:07 UTC
\`\`\`

После этого момента 32-битные системы могут показать 1901 год или вызвать сбой.

---

## 4. Строки и текстовые данные

### Длина строки

Каждое текстовое поле имеет ограничения:

| Контекст | Типичный лимит |
|----------|----------------|
| Имя пользователя | 3-50 символов |
| Email | До 254 символов (RFC 5321) |
| URL | До 2048 символов (практический лимит) |
| Текстовое поле БД | VARCHAR(255), TEXT, и т.д. |

### Граничные значения для строк

| Значение | Тип границы |
|----------|-------------|
| \`""\` (пустая строка) | Минимум |
| \`" "\` (только пробелы) | Почти пустая |
| 1 символ | MIN для многих полей |
| Точно MAX символов | Верхняя граница |
| MAX + 1 символ | Превышение |
| 10,000+ символов | Стресс-тест |

### Специальные символы

Некоторые символы имеют особое значение в разных контекстах:

| Символ | Проблема |
|--------|----------|
| \`'\` (одинарная кавычка) | SQL injection |
| \`"\` (двойная кавычка) | JSON/строки |
| \`<\` \`>\` | HTML/XML injection |
| \`\\\\\` (обратный слеш) | Escape-последовательности |
| \`\\\\0\` (null byte) | Терминатор строки в C |
| \`%00\` | URL-encoded null byte |
| \`\\\\n\` \`\\\\r\` | Переносы строк |
| Emoji 🎉 | Многобайтовые символы |

### Unicode и кодировки

Мир не ограничивается ASCII:

\`\`\`
ASCII:     A = 0x41 (1 байт)
UTF-8:     A = 0x41 (1 байт)
           Ж = 0xD0 0x96 (2 байта)
           中 = 0xE4 0xB8 0xAD (3 байта)
           🎉 = 0xF0 0x9F 0x8E 0x89 (4 байта)
\`\`\`

**Вопрос для размышления:** Если лимит поля — 255 символов, то 255 эмодзи займут 1020 байт. Как система это обработает?

### Homoglyph атаки

Некоторые символы выглядят одинаково, но имеют разные коды:

\`\`\`
"а" (кириллица, U+0430) vs "a" (латиница, U+0061)
"е" (кириллица, U+0435) vs "e" (латиница, U+0065)
\`\`\`

Это используется в фишинге: \`аpple.com\` выглядит как \`apple.com\`, но это разные домены.

---

## 5. Пагинация и лимиты

### Типичные параметры пагинации

\`\`\`
GET /api/v1/items?page=1&limit=20
GET /api/v1/items?offset=0&limit=20
\`\`\`

### Граничные значения пагинации

| Параметр | Тест | Потенциальная проблема |
|----------|------|------------------------|
| \`page=0\` | Нулевая страница | Некорректная выборка |
| \`page=-1\` | Отрицательная страница | SQL injection, crash |
| \`page=999999\` | Несуществующая страница | Пустой результат или ошибка |
| \`limit=0\` | Нулевой лимит | Пустой результат или все записи |
| \`limit=-1\` | Отрицательный лимит | Все записи или ошибка |
| \`limit=1000000\` | Огромный лимит | DoS, timeout, OOM |
| \`offset=-100\` | Отрицательный offset | Некорректная выборка |

### Атака через пагинацию

Если API не ограничивает \`limit\`, злоумышленник может:

\`\`\`
GET /api/v1/users?limit=10000000
\`\`\`

Это может вызвать:
- Перегрузку базы данных
- Исчерпание памяти сервера
- Denial of Service (DoS)

---

## 6. Вычисления и округление

### Проблемы округления

Как округлить 2.5?

| Метод | Результат | Описание |
|-------|-----------|----------|
| Round Half Up | 3 | Классическое математическое |
| Round Half Down | 2 | Округление к меньшему |
| Round Half Even | 2 | «Банковское» округление |
| Truncate | 2 | Просто отбросить дробную часть |

**Банковское округление** (Round Half Even) используется в финансах, чтобы избежать систематического завышения при множестве операций.

### Пример: скидки и итоги

\`\`\`
Товар: $9.99
Скидка: 15%

Расчёт скидки: 9.99 × 0.15 = 1.4985
Округлённая скидка: $1.50

Итог: 9.99 - 1.50 = $8.49
\`\`\`

Но что если округлить иначе?

\`\`\`
Округление вниз: 1.4985 → $1.49
Итог: 9.99 - 1.49 = $8.50
\`\`\`

Разница в 1 цент на транзакцию × миллионы транзакций = серьёзные суммы.

### Накопление ошибок

При множестве операций погрешности накапливаются:

\`\`\`
100 товаров по $0.01 со скидкой 50%

Ожидание: 100 × 0.01 × 0.5 = $0.50

Реальность (с округлением каждого товара):
0.01 × 0.5 = 0.005 → округляется до 0.01 или 0.00
100 × 0.00 = $0.00 (если округление вниз)
100 × 0.01 = $1.00 (если округление вверх)
\`\`\`

---

## 7. Практический пример: API управления запасами

Рассмотрим API системы учёта товаров на складе.

### Спецификация

\`\`\`yaml
/api/v1/inventory/{itemId}:
  patch:
    summary: Update item quantity
    requestBody:
      content:
        application/json:
          schema:
            type: object
            properties:
              quantity:
                type: integer
                minimum: 0
                maximum: 999999
              unitPrice:
                type: number
                format: decimal
                minimum: 0.01
                maximum: 99999.99
              expiryDate:
                type: string
                format: date
\`\`\`

### Направления для исследования

Изучая эту спецификацию, тестировщик может задуматься:

**Числовые границы:**
- Что на границах \`quantity\`? 0, 999999, 1000000?
- Как обрабатывается \`unitPrice\` с разной точностью?
- Что если \`unitPrice\` = 0?

**Форматы дат:**
- Принимается ли дата в прошлом?
- Как обрабатываются разные временные зоны?
- Что с невалидными датами (30 февраля)?

**Вычисления:**
- Как рассчитывается общая стоимость (quantity × unitPrice)?
- Что происходит при очень больших quantity и unitPrice?

**Комбинации:**
- Что если все поля на граничных значениях одновременно?

---

## 8. Чек-лист исследователя T2

### Числовые поля

- [ ] Проверить MIN и MAX из документации
- [ ] Попробовать значения за пределами (MIN-1, MAX+1)
- [ ] Тестировать overflow (очень большие числа)
- [ ] Проверить отрицательные значения
- [ ] Тестировать ноль
- [ ] Проверить точность десятичных чисел

### Даты и время

- [ ] Проверить граничные даты (конец года, конец месяца)
- [ ] Тестировать високосные годы
- [ ] Проверить работу с временными зонами
- [ ] Попробовать даты в далёком прошлом и будущем
- [ ] Тестировать невалидные форматы

### Строки

- [ ] Проверить пустую строку
- [ ] Тестировать строку из пробелов
- [ ] Проверить максимальную длину
- [ ] Тестировать превышение длины
- [ ] Попробовать специальные символы
- [ ] Проверить Unicode и эмодзи

### Пагинация

- [ ] Проверить page=0 и page=-1
- [ ] Тестировать очень большие номера страниц
- [ ] Проверить limit=0 и отрицательные значения
- [ ] Тестировать очень большие limit

### Вычисления

- [ ] Проверить округление на граничных значениях
- [ ] Тестировать накопление ошибок
- [ ] Проверить расчёты с очень большими числами
- [ ] Тестировать деление на ноль

---

## 9. Реальные примеры багов из практики

### Boeing 787 — Integer Overflow

В 2015 году обнаружилось, что 787 Dreamliner может потерять управление, если непрерывно работает более 248 дней. Причина: 32-битный счётчик времени переполнялся.

### Ariane 5 — Type Conversion

В 1996 году ракета Ariane 5 взорвалась через 37 секунд после старта. Причина: 64-битное число было преобразовано в 16-битное, вызвав overflow.

### Therac-25 — Race Condition + Overflow

Медицинский ускоритель из-за переполнения счётчика облучал пациентов смертельными дозами радиации.

---

## Заключение

T2 миссии требуют более глубокого понимания того, как данные хранятся и обрабатываются. Помните:

> **«Границы — это не стены, а окна в скрытые баги»**

Каждое поле ввода имеет свои ограничения. Ваша задача — найти, где эти ограничения проверяются некорректно или не проверяются вовсе.

Успехов в исследовании границ!

---

## Полезные ресурсы

- [IEEE 754 Floating Point](https://en.wikipedia.org/wiki/IEEE_754) — стандарт чисел с плавающей точкой
- [ISO 8601 Date/Time](https://en.wikipedia.org/wiki/ISO_8601) — стандарт дат
- [Unicode](https://unicode.org/) — спецификация символов
- [OWASP Testing Guide](https://owasp.org/www-project-web-security-testing-guide/) — тестирование безопасности

---

*Этот теоретический блок является частью QA Training Platform. Материал подготавливает к исследованию граничных значений и форматов данных в API.*`;

// Заглушки для будущих тиров
export const T3_THEORY = null; // Будет добавлено позже
export const T4_THEORY = null; // Будет добавлено позже
export const T5_THEORY = null; // Будет добавлено позже

// Маппинг тиров на контент
export const THEORY_CONTENT = {
  T1: T1_THEORY,
  T2: T2_THEORY,
  T3: T3_THEORY,
  T4: T4_THEORY,
  T5: T5_THEORY,
};
